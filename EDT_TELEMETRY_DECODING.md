# EDT (Extended DShot Telemetry) декодування на прошивці AM32 2.20

> **Ціль:** зрозуміти формат кадру EDT, який відправляє ESC на прошивці **AM32 2.20** після команди `DShot cmd 13`, та коректно декодувати eRPM / температуру / напругу / струм у коді ESP32 (`dshot_rmt.c`, `esc_dshot.c`).

---

## 1. Протокол DShot (база)

DShot — цифровий протокол керування ESC зі швидкістю 300/600/1200 kb/s. Кожен кадр містить **16 біт**:

```
[двійкові 12 біт даних][telemetry bit][4-бітний CRC (XOR, з яких передаються 3)]
```

| Біти | Призначення |
| :--- | :--- |
| 15..4 | 12-бітні дані (пакет/команда/телеметрія) |
| 3 | telemetry bit (запит ESC на відповідь телеметрією) |
| 2..0 | CRC (XOR по ніблах, інверсія, `&0xf`) |

**Газ ESP32 → ESC:** значення 0–47 = команди DShot, 48–2047 = газ (11 біт).

---

## 2. EDT (Extended DShot Telemetry)

EDT — розширення DShot, яке передає напругу, струм, температуру та eRPM тим самим одним сигнальним проводом за принципом «запитав–відповів», використовуючи bidirectional режим піна (TX→RX між кадрами).

### 2.1 Як ввімкнути

EDT вмикається **пачкою кадрів** з командою **`DShot 13`**. AM32 виконує цифрову команду тільки після **6 послідовних однакових кадрів**; команда 13 (як і 1–5, beacons) виконується відразу.

- Вмикання: `cmd 13` → `dshot_extended_telemetry = 1`, надсилається init-кадр `0b111000000000`.
- Вимкнення: `cmd 14` → `dshot_extended_telemetry = 0`, надсилається deinit-кадр `0b111011111111`.

У `esc_dshot.c` це зроблено функціями `esc_dshot_enable_edt()` / `esc_dshot_disable_edt()` (пачка з 8 кадрів `cmd 13`).

---

## 3. Структура кадру EDT (відповіді ESC)

Кадр відповіді ESC має формат:

```
[ 12 біт даних ][ 4 біт CRC ]
```

Старші 4 біти 12-бітного значення — **прапорець типу телеметрії**, молодші 8 біт — значення.

| Старший напівбайт | Тип телеметрії | Молодші 8 біт (значення) |
| :--- | :--- | :--- |
| `0b0100` (0x4) | **Напруга** | `battery_voltage / 25` |
| `0b0110` (0x6) | **Струм** | `actual_current / 50` |
| `0b0010` (0x2) | **Температура** | `degrees_celsius` |
| інше | **eRPM** | комутаційний час (eRPM) з плаваючим масштабом |

Тип кодується в 4 біти (біти 11..8 у 12-бітному значенні), тому при декодуванні беремо **`value >> 8`** (0x2 / 0x4 / 0x6).

Код генерації в AM32 (`dshot.c`):

```c
extended_frame_to_send = 0b0110 << 8 | (uint8_t)(actual_current / 50);   // струм
extended_frame_to_send = 0b0100 << 8 | (uint8_t)(battery_voltage / 25);  // напруга
extended_frame_to_send = 0b0010 << 8 | degrees_celsius;                  // температура
```

---

## 4. Масштабування (AM32 2.20) — важливо для нашого коду

| Телеметрія | Формула в AM32 | Дільник | 1 LSB = |
| :--- | :--- | :--- | :--- |
| Напруга | `battery_voltage / 25` | 25 | 0.25 В |
| Струм | `actual_current / 50` | 50 | 0.5 А |
| Температура | `degrees_celsius` | 1 | 1 °C |

> [!IMPORTANT] У **master-версії** AM32 масштаб струму інший (`/100`, тобто 1 LSB = 1.0 A). У **AM32 2.20** — `/50` (0.5 A). У `dshot_rmt.c` виправлено `case 0x06` на `raw * 50`.

### eRPM
Значення eRPM передається як комутаційний час; фактичний eRPM розраховується з оберненої залежності від періоду та полюсів мотора. Коефіцієнт залежить від моделі ESC та прошивки, тому підбирається емпірично (`erpm = div / (10000 - comm_time)` тощо).

---

## 5. Планувальник телеметрії AM32

У `make_dshot_package()` AM32 чергує кадри EDT через `telem_scheduler`, щоб між ними залишалися кадри eRPM. Після будь-якого EDT-кадра наступний завжди — eRPM.

```c
#define TEMP_EDT_RATE_DIVISOR    200   // температура
#define VOLTAGE_EDT_RATE_DIVISOR 200   // напруга
#define CURRENT_EDT_RATE_DIVISOR 40    // струм (частіше)
```

Точні частоти залежать від вхідної частоти DShot (напр. при 800 Гц — темп/напр. ≈ 4 Гц, струм ≈ 20 Гц).

---

## 6. CRC та GCR кодування

### 6.1 CRC (4 біти)

Сторона генерації (AM32):

```c
uint16_t csum = 0;
uint16_t csum_data = dshot_full_number;
for (int i = 0; i < 3; i++) {
    csum ^= csum_data;      // XOR даних по ніблах
    csum_data >>= 4;
}
csum = ~csum;               // інверсія
csum &= 0xf;
dshot_full_number = (dshot_full_number << 4) | csum; // 12 → 16 біт
```

Сторона декодування (ESP32):

```c
crc = (packet >> 4) ^ (packet >> 8) ^ (packet >> 12) ^ (packet & 0xf);
// crc == 0xf → пакет валідний
```

### 6.2 GCR (RLL 4-біт → 5-біт)

AM32 кодує 16-бітний пакет у 20 біт за таблицею **GCR (Gibson code RLL)**. На стороні ESP32 це робиться у `dshot_rmt.c` (декодер GCR).

| Нібл | GCR (AM32 `gcr_encode_table`) |
| :--- | :--- |
| 0 | `0b11001` |
| 1 | `0b11011` |
| 2 | `0b10010` |
| 3 | `0b10011` |
| 4 | `0b11101` |
| 5 | `0b10101` |
| 6 | `0b10110` |
| 7 | `0b10111` |
| 8 | `0b11010` |
| 9 | `0b01001` |
| 10 | `0b01010` |
| 11 | `0b01011` |
| 12 | `0b11110` |
| 13 | `0b01101` |
| 14 | `0b01110` |
| 15 | `0b01111` |

---

## 7. Схема декодування в ESP32

1. Зняти 20 біт GCR зі входу (RMT RX з loopback / bidirectional піном).
2. Декодувати GCR → 16-бітний `packet`.
3. Врахувати напрям/інверсію сигналу (bidirectional DShot).
4. Перевірити CRC (розділ 6.1).
5. Визначити тип за `value >> 8` і розкодувати:
   - `0x2` → `temp = raw`
   - `0x4` → `voltage = raw * 0.25`
   - `0x6` → `current = raw * 0.5` *(AM32 2.20)*
   - інше → eRPM.

---

## 8. Джерела

- **Специфікація EDT:** [bird-sanctuary / extended-dshot-telemetry](https://github.com/bird-sanctuary/extended-dshot-telemetry)
- **AM32 dshot.c:** https://github.com/am32-firmware/AM32/blob/master/Src/dshot.c
- **AM32 deepwiki — DShot protocol:** https://deepwiki.com/am32-firmware/AM32/4.1-dshot-protocol-implementation

---

## 9. Чек-лист для реалізації/перевірки в ESP32

- [ ] `esc_dshot_enable_edt()` відправляє пачку з 8 кадрів `cmd 13`.
- [ ] RX-канал перемикається на TX між кадрами (open-drain, `io_loop_back`).
- [ ] CRC-декодування відповідає формулі AM32.
- [ ] Масштаби: напруга `0.25`, струм `0.5`, температура `1.0` (AM32 2.20).
- [ ] Після зміни протоколу (PWM → Bidir DShot) ESC переармований і EDT повторно ввімкнено.
